def solution(s):
    answer = True
    str.isdigit(s)
    if len(s) == 4 and 6:
        try:
            type(int(s)) == int

        except ValueError:
            answer = False
    else:
        answer = False
    return answer
